import React from 'react';

const Filterbutton = (props) => {
    return (
        
      <button type="button">
      <span >Show </span>
      <span>all </span>
      <span > tasks</span>
    </button>
    );
};

export default Filterbutton;